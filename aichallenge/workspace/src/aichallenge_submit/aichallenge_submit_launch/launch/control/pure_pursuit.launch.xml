<?xml version="1.0" encoding="UTF-8"?>
<launch>
  <arg name="simulation" default="true"/>
  <arg name="use_sim_time" default="true"/>

  <let name="steering_tire_angle_gain_var" value="1.50" if="$(var simulation)"/>
  <let name="steering_tire_angle_gain_var" value="1.639" unless="$(var simulation)"/>

  <node pkg="simple_pure_pursuit" exec="simple_pure_pursuit" name="simple_pure_pursuit_node" output="screen">
    <param name="use_sim_time" value="$(var use_sim_time)"/>
    <param name="use_external_target_vel" value="true"/>
    <param name="external_target_vel" value="6.222"/>
    <param name="lookahead_gain" value="0.5"/>
    <param name="lookahead_min_distance" value="3.5"/>
    <param name="speed_proportional_gain" value="1.0"/>
    <param name="steering_tire_angle_gain" value="$(var steering_tire_angle_gain_var)"/>
    <!-- imu_gnss_poser maps a poor AWSIM GNSS fix to covariance 100.0.
         Accept that defined fallback value so the controller can move off the
         grid; the check in simple_pure_pursuit rejects only values above it. -->
    <param name="max_gnss_position_covariance" value="100.0"/>

    <remap from="input/kinematics" to="/localization/kinematic_state"/>
    <remap from="input/gnss_pose" to="/localization/imu_gnss_poser/pose_with_covariance"/>
    <remap from="input/trajectory" to="/planning/scenario_planning/trajectory"/>
    <remap from="output/control_cmd" to="/control/command/control_cmd"/>
  </node>
</launch>
