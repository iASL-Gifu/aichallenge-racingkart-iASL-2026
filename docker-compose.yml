x-autoware-base: &autoware-base
  image: "aichallenge-2025-dev"
  privileged: true
  pull_policy: never
  network_mode: host
  stop_grace_period: 0.1s
  environment:
    - DISPLAY=${DISPLAY}
    - USER=${USER}
    - ROS_DISTRO=humble
    - XAUTHORITY=${XAUTHORITY}
    - QT_X11_NO_MITSHM=1
    - TZ=Asia/Tokyo
    - RUN_MODE=${RUN_MODE:-}
    - SIM_MODE=${SIM_MODE:-}
    - CMD=${CMD:-}
    - COLCON_TRACE=${COLCON_TRACE:-}
    - AIC_CAPTURE=${AIC_CAPTURE:-false}
    - AIC_ROSBAG=${AIC_ROSBAG:-false}
    # GPU (set by Makefile when DEVICE enables GPU, otherwise empty)
    - NVIDIA_VISIBLE_DEVICES=${NVIDIA_VISIBLE_DEVICES:-}
    - NVIDIA_DRIVER_CAPABILITIES=${NVIDIA_DRIVER_CAPABILITIES:-}
  volumes:
    - ./output:${OUTPUT_ROOT:-/output}
    - ./aichallenge:/aichallenge
    - ./remote:/remote
    - ./vehicle:/vehicle
    - /tmp/.X11-unix:/tmp/.X11-unix:rw
    # Audio (PipeWire/Pulse) socket access on the host
    - /run/user:/run/user:rw
    - /dev/dri:/dev/dri
    - ${XAUTHORITY}:${XAUTHORITY}:rw
  devices:
    - /dev/dri
    - /dev/video0
    - /dev/input
  working_dir: /aichallenge

x-racing_kart_interface-base: &racing_kart_interface-base
  image: "ghcr.io/tier4/racing_kart_interface:latest-experiment"
  privileged: true
  pull_policy: never
  network_mode: host
  stop_grace_period: 0.1s
  environment:
    - DISPLAY=${DISPLAY}
    - USER=${USER}
    - TZ=Asia/Tokyo
    - ROS_DISTRO=humble
    - NTRIP_USERNAME=${NTRIP_USERNAME:-}
    - NTRIP_PASSWORD=${NTRIP_PASSWORD:-}
  volumes:
    - /dev/vcu:/dev/vcu
    - /dev/gnss:/dev/gnss
    - /tmp/.X11-unix:/tmp/.X11-unix:rw
    - /dev/dri:/dev/dri
  devices:
    - /dev/dri
  group_add:
    - dialout
  working_dir: /workspace

services:
  # Autoware service
  autoware:
    <<: *autoware-base 
    working_dir: ${OUTPUT_RUN_DIR:-/aichallenge}
    command: ["bash", "-lc", "exec /aichallenge/run_autoware.bash ${RUN_MODE:-awsim} ${DOMAIN_ID:-1} >\"${OUTPUT_RUN_DIR:-/output}/autoware.log\" 2>&1"]

  # Autoware service
  autoware-build:
    <<: *autoware-base 
    command: ["bash", "-lc", "exec bash /aichallenge/build_autoware.bash ${HOST_UID:-} ${HOST_GID:-}"]

  simulator:
    <<: *autoware-base 
    working_dir: ${OUTPUT_RUN_DIR:-/aichallenge}
    command: ["bash", "-lc", "exec env PULSE_SERVER=unix:/run/user/${HOST_UID:-1000}/pulse/native /aichallenge/run_simulator.bash ${SIM_MODE:-} >\"${OUTPUT_RUN_DIR:-/output}/awsim.log\" 2>&1"]

  autoware-command:
    <<: *autoware-base
    command: ["bash", "-lc", "source /aichallenge/workspace/install/setup.bash && cd ${CMD_WORKDIR:-/aichallenge} && exec bash -lc \"${CMD:-}\""]

### Autoware Service For Parallel Execution ###
  autoware-d1:
    <<: *autoware-base 
    image: "autoware-d1"
    command: ["bash", "-lc", "exec /aichallenge/run_autoware.bash ${RUN_MODE:-awsim} 1 >\"${OUTPUT_RUN_DIR:-/output/d1}/autoware.log\" 2>&1"]

  autoware-d2:
    <<: *autoware-base 
    image: "autoware-d2"
    command: ["bash", "-lc", "exec /aichallenge/run_autoware.bash ${RUN_MODE:-awsim} 2 >\"${OUTPUT_RUN_DIR:-/output/d2}/autoware.log\" 2>&1"]

  autoware-d3:
    <<: *autoware-base
    image: "autoware-d3"
    command: ["bash", "-lc", "exec /aichallenge/run_autoware.bash ${RUN_MODE:-awsim} 3 >\"${OUTPUT_RUN_DIR:-/output/d3}/autoware.log\" 2>&1"]

  autoware-d4:
    <<: *autoware-base
    image: "autoware-d4"
    command: ["bash", "-lc", "exec /aichallenge/run_autoware.bash ${RUN_MODE:-awsim} 4 >\"${OUTPUT_RUN_DIR:-/output/d4}/autoware.log\" 2>&1"]

#### vehicle specific services can be added here ####
  # Zenoh service
  zenoh:
    <<: *autoware-base
    working_dir: /vehicle
    command: ["bash", "-c", "case ${VEHICLE_ID:-} in A2) PORT=7448;; A3) PORT=7449;; A6) PORT=7450;; A7) PORT=7451;; *) echo 'Invalid VEHICLE_ID'; exit 1;; esac && zenoh-bridge-ros2dds client -e tls/zenoh.dev.aichallenge-board.jsae.or.jp:$$PORT -c /vehicle/zenoh.json5"]

  # Driver service
  driver:
    <<: *racing_kart_interface-base
    command: ['vehicle']

#### remote visualization services ####
  rviz2:
    <<: *autoware-base 
    command: ["bash", "-c", "cd /aichallenge && exec bash utils/run_rviz.bash remote"]
